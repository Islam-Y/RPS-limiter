apiVersion: 1

providers:
  - name: RPSLimiterDashboards
    orgId: 1
    folder: RPS Limiter
    type: file
    disableDeletion: false
    editable: false
    updateIntervalSeconds: 15
    options:
      path: /etc/grafana/provisioning/dashboards/json
